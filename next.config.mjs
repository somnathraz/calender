/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["pixabay.com"], // Add the domain hosting your images
  },
};

export default nextConfig;
